# Workspace packages must be declared as dependencies to be importable.
# This is what makes the dependency graph in IMPLEMENTATION.md §2 real rather
# than advisory: a package cannot reach a sibling it did not declare.
auto-install-peers=true
strict-peer-dependencies=false
resolution-mode=highest
