diff a/apps/web/app/globals.css b/apps/web/app/globals.css	(rejected hunks)
@@ -19,8 +19,11 @@
 }
 
 @theme inline {
-  --font-heading: var(--font-sans);
-  --font-sans: var(--font-sans);
+  /* `geist/font/sans` sets --font-geist-sans on <html> via GeistSans.variable.
+     Referencing it by name rather than re-declaring --font-sans keeps these
+     utilities pointing at the self-hosted face. */
+  --font-heading: var(--font-geist-sans);
+  --font-sans: var(--font-geist-sans);
   --color-sidebar-ring: var(--sidebar-ring);
   --color-sidebar-border: var(--sidebar-border);
   --color-sidebar-accent-foreground: var(--sidebar-accent-foreground);
