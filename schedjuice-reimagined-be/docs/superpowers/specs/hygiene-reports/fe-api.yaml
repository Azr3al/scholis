slice_id: fe-api
repo: schedjuice-reimagined-fe
path: src/api
skipped: false
deleted: []
fixed_docs: []
anti_pattern_fixes: []
ambiguous:
  - path: src/api/rbac.ts
    symbol: RBAC_CATALOG_KEY
    reason: possible-dead-export
    confidence: ambiguous
    notes: >-
      Exported TanStack Query key; used only inside rbac.ts (useCatalog).
      Zero external static imports. Likely intentional public cache API for
      invalidate/prefetch — do not un-export without confirm.
  - path: src/api/rbac.ts
    symbol: RBAC_ROLES_KEY
    reason: possible-dead-export
    confidence: ambiguous
    notes: >-
      Exported TanStack Query key; used only inside rbac.ts (useRoles +
      invalidateRbacQueries). Zero external static imports. Same public-cache
      surface as RBAC_CATALOG_KEY — leave alone pending review.
files_touched: []
verification:
  command: pnpm exec vitest run --config vitest.config.mts src/api
  passed: true
  skipped: true
  notes: >-
    No test files under src/api (only rbac.ts). Scoped Vitest exits 1 with
    "No test files found" — verification skipped as N/A for this slice.
    Note: `pnpm test:unit -- src/api` still collects the full unit suite under
    this Vitest config (pre-existing failures outside src/api).
    All hooks/types have static callers; no FE files edited.
