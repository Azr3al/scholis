slice_id: fe-lib-certifications
repo: schedjuice-reimagined-fe
path: src/lib/certifications
skipped: false
deleted: []
fixed_docs: []
anti_pattern_fixes: []
ambiguous:
  - path: src/lib/certifications/visibility.ts
    symbol: canViewCertificationsSection.tenant
    reason: unused-context-param
    confidence: ambiguous
    notes: >-
      Optional tenant on the param type (and organizationType import) is never
      destructured or read. Sibling canViewAiSection omits tenant; canViewPointsSection
      uses it for is_staff_points_enabled. May be intentional RecordSectionContext
      shape parity — do not strip without confirm.
files_touched:
  - docs/superpowers/specs/hygiene-reports/fe-lib-certifications.yaml
verification:
  command: pnpm exec vitest run --config vitest.config.mts src/lib/certifications
  passed: true
  skipped: true
  notes: >-
    Vitest exit 1: "No test files found" under src/lib/certifications — treated
    as skip. canViewCertificationsSection is live via record-sections
    RECORD_SECTIONS. No FE source edits (scan-only; no obvious dead
    exports/modules/docs).
