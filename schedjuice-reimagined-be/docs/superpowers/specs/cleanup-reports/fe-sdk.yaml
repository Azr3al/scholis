slice_id: fe-sdk
repo: schedjuice-reimagined-fe
path: src/sdk
skipped: false
deleted: []
replaced: []
ambiguous:
  - path: src/sdk/__tests__/envelope.test.ts
    test_name: unwrapList reads rows and count from schedjuice list envelope
    reason: may-be-contrast-anchor
    notes: >
      Thin primary count→total path; siblings cover total alias and
      total_pages*pageSize last-resort. Likely intentional contrast for
      production envelope preference over fallbacks.
  - path: src/sdk/__tests__/define-search-list.test.ts
    test_name: defineSearchListResource posts to path/search and unwraps rows/total
    reason: may-be-composition-smoke
    notes: >
      Sole factory coverage; asserts search wiring (pageSize→size, empty
      filter body) plus keys.list defaults. Partially composes unwrapList /
      toFilterBody already tested in-file; unclear if keys shape assert is
      tautology of listKeyPayload vs intentional cutover contract.
files_touched:
  - docs/superpowers/specs/cleanup-reports/fe-sdk.yaml
verification:
  command: pnpm exec vitest run --config vitest.config.mts src/sdk
  passed: true
counts:
  deleted: 0
  replaced: 0
  ambiguous: 2
  tests_before: 6
  tests_after: 6
