diff a/schedjuice-reimagined-be/app_finance/tests/test_student_checkout.py b/schedjuice-reimagined-be/app_finance/tests/test_student_checkout.py	(rejected hunks)
@@ -13,7 +13,13 @@ from tenant_schemas.utils import schema_context
 
 from app_auth.models import User
 from app_course.models import Category, Course, Program
-from app_finance.models import PaymentPlan, UserPayment, UserPaymentGroup
+from app_finance.models import (
+    PaymentBank,
+    PaymentMethod,
+    PaymentPlan,
+    UserPayment,
+    UserPaymentGroup,
+)
 from app_finance.tests.billing_base import BillingTenantAwareBaseTest
 
 
@@ -65,6 +71,10 @@ class StudentCheckoutTest(BillingTenantAwareBaseTest):
                 start_date=today,
                 end_date=today + timedelta(days=30),
             )
+            cls.kpay = PaymentMethod.objects.create(
+                name=f"KPay checkout {suffix}",
+                payment_bank=PaymentBank.KPAY,
+            )
 
     def _login(self):
         res = self.client.post(
@@ -175,3 +185,133 @@ class StudentCheckoutTest(BillingTenantAwareBaseTest):
             **self._auth(token),
         )
         self.assertEqual(res.status_code, 403)
+
+    @patch("app_finance.ocr_event_log.resolve_ocr_correctness_on_create.delay")
+    @patch("app_finance.views.extract_receiver_ss_text_data.delay")
+    def test_legacy_submit_skips_ocr_when_txn_and_amount_prefilled(
+        self, mock_extract, mock_resolve
+    ):
+        payment = self._pending_payment(self.course_a, "50000")
+        token = self._login()
+        txn = "11111111111111111111"
+        res = self.client.post(
+            reverse("make-payment"),
+            {
+                "id": str(payment.id),
+                "screenshot": _tiny_png(),
+                "parsed_amount": "50000",
+                "transaction_id": txn,
+            },
+            format="multipart",
+            **self._auth(token),
+        )
+        self.assertEqual(res.status_code, 200, res.content)
+        with schema_context(self.schema_name):
+            payment.refresh_from_db()
+            self.assertEqual(payment.status, UserPayment.Status.PENDING_VERIFICATION)
+            self.assertEqual(payment.transaction_id, txn)
+        mock_extract.assert_not_called()
+        mock_resolve.assert_not_called()
+
+    @patch("app_finance.views.extract_receiver_ss_text_data.delay")
+    def test_legacy_submit_enqueues_ocr_when_screenshot_has_no_amount(
+        self, mock_extract
+    ):
+        payment = self._pending_payment(self.course_a, "50000")
+        token = self._login()
+        res = self.client.post(
+            reverse("make-payment"),
+            {
+                "id": str(payment.id),
+                "screenshot": _tiny_png(),
+            },
+            format="multipart",
+            **self._auth(token),
+        )
+        self.assertEqual(res.status_code, 200, res.content)
+        with schema_context(self.schema_name):
+            payment.refresh_from_db()
+            self.assertEqual(payment.status, UserPayment.Status.AWAITING_EXTRACTION)
+        mock_extract.assert_called_once_with(payment.id, self.schema_name)
+
+    @patch("app_finance.ocr_event_log.resolve_ocr_correctness_on_create.delay")
+    @patch("app_finance.views.extract_receiver_ss_text_data.delay")
+    def test_legacy_submit_resolves_ocr_event_when_prefilled(
+        self, mock_extract, mock_resolve
+    ):
+        payment = self._pending_payment(self.course_a, "50000")
+        token = self._login()
+        txn = "22222222222222222222"
+        res = self.client.post(
+            reverse("make-payment"),
+            {
+                "id": str(payment.id),
+                "screenshot": _tiny_png(),
+                "parsed_amount": "50000",
+                "transaction_id": txn,
+                "ocr_event_id": "aaaaaaaa-bbbb-cccc-dddd-eeeeeeeeeeee",
+            },
+            format="multipart",
+            **self._auth(token),
+        )
+        self.assertEqual(res.status_code, 200, res.content)
+        mock_extract.assert_not_called()
+        mock_resolve.assert_called_once()
+        kwargs = mock_resolve.call_args.kwargs
+        self.assertEqual(kwargs["event_id"], "aaaaaaaa-bbbb-cccc-dddd-eeeeeeeeeeee")
+        self.assertEqual(kwargs["schema_name"], self.schema_name)
+        self.assertEqual(kwargs["user_payment_id"], payment.id)
+        self.assertEqual(kwargs["submitted_transaction_id"], txn)
+        self.assertEqual(kwargs["submitted_amount"], Decimal("50000"))
+
+    @patch("app_finance.views.extract_receiver_ss_text_data.delay")
+    def test_multi_course_checkout_skips_ocr_when_txn_and_amount_prefilled(
+        self, mock_extract
+    ):
+        payment_a = self._pending_payment(self.course_a, "60000")
+        payment_b = self._pending_payment(self.course_b, "40000")
+        token = self._login()
+        txn = "33333333333333333333"
+        res = self.client.post(
+            reverse("make-payment"),
+            {
+                "checkout": "1",
+                "payment_ids_count": "2",
+                "payment_id_0": str(payment_a.id),
+                "payment_id_1": str(payment_b.id),
+                "screenshots_count": "1",
+                "screenshot_0_screenshot": _tiny_png(),
+                "screenshot_0_parsed_amount": "50000",
+                "screenshot_0_transaction_id": txn,
+            },
+            format="multipart",
+            **self._auth(token),
+        )
+        self.assertEqual(res.status_code, 200, res.content)
+        mock_extract.assert_not_called()
+        with schema_context(self.schema_name):
+            payment_a.refresh_from_db()
+            payment_b.refresh_from_db()
+            self.assertEqual(payment_a.status, UserPayment.Status.PENDING_VERIFICATION)
+            self.assertEqual(payment_b.status, UserPayment.Status.PENDING_VERIFICATION)
+
+    @patch("app_finance.views.extract_receiver_ss_text_data.delay")
+    def test_legacy_submit_persists_suggested_payment_method(self, _mock_extract):
+        payment = self._pending_payment(self.course_a, "50000")
+        token = self._login()
+        res = self.client.post(
+            reverse("make-payment"),
+            {
+                "id": str(payment.id),
+                "screenshot": _tiny_png(),
+                "parsed_amount": "50000",
+                "transaction_id": "44444444444444444444",
+                "payment_method": str(self.kpay.id),
+            },
+            format="multipart",
+            **self._auth(token),
+        )
+        self.assertEqual(res.status_code, 200, res.content)
+        with schema_context(self.schema_name):
+            payment.refresh_from_db()
+            self.assertEqual(payment.payment_method_id, self.kpay.id)
