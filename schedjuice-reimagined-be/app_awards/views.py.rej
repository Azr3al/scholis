diff a/schedjuice-reimagined-be/app_awards/views.py b/schedjuice-reimagined-be/app_awards/views.py	(rejected hunks)
@@ -1,5 +1,7 @@
 from django.utils import timezone
+from rest_framework import status
 from rest_framework.exceptions import ValidationError
+from rest_framework.response import Response
 
 from app_auth.models import User
 from app_awards import models, serializers, services
@@ -230,11 +232,13 @@ def _template_payload(template, request):
     ).data
 
 
-class AwardTitleTemplateListView(RBACView):
-    name = "Award title templates"
+class AwardTitleCertificateView(RBACView):
+    name = "Award title certificate"
     required_permissions = {
         "GET": "award_title.manage",
         "POST": "award_title.manage",
+        "PATCH": "award_title.manage",
+        "DELETE": "award_title.manage",
     }
 
     def _title(self, obj_id: int):
@@ -248,58 +252,46 @@ class AwardTitleTemplateListView(RBACView):
             return self.validation_error(
                 {"title": "Local titles cannot have templates."}
             )
-        qs = models.AwardTemplate.objects.filter(title=title).order_by("name")
-        return self.ok(
-            serializers.AwardTemplateSerializer(
-                qs, many=True, context={"request": request}
-            ).data
-        )
+        template = services.get_award_certificate(title)
+        if template is None:
+            return self.not_found("Certificate not found.")
+        return self.ok(_template_payload(template, request))
 
     def post(self, request, obj_id: int):
         title = self._title(obj_id)
         if title is None:
             return self.not_found("Award title not found.")
-        body = request.data or {}
-        name = body.get("name")
         try:
-            template = services.create_award_template(
-                title, name, acting_user(request)
-            )
+            template = services.create_award_certificate(title, acting_user(request))
         except ValidationError as exc:
-            return self.validation_error(exc.detail)
+            detail = exc.detail
+            if isinstance(detail, dict) and "certificate" in detail:
+                return Response(
+                    {
+                        "isError": True,
+                        "message": "conflict",
+                        "details": detail,
+                    },
+                    status=status.HTTP_409_CONFLICT,
+                )
+            return self.validation_error(detail)
         return self.created(_template_payload(template, request))
 
-
-class AwardTemplateDetailsView(RBACView):
-    name = "Award template details"
-    required_permissions = {
-        "GET": "award_title.manage",
-        "PATCH": "award_title.manage",
-        "DELETE": "award_title.manage",
-    }
-
-    def _template(self, obj_id: int):
-        return models.AwardTemplate.objects.filter(pk=obj_id).first()
-
-    def get(self, request, obj_id: int):
-        template = self._template(obj_id)
-        if template is None:
-            return self.not_found("Award template not found.")
-        return self.ok(_template_payload(template, request))
-
     def patch(self, request, obj_id: int):
-        template = self._template(obj_id)
+        title = self._title(obj_id)
+        if title is None:
+            return self.not_found("Award title not found.")
+        template = services.get_award_certificate(title)
         if template is None:
-            return self.not_found("Award template not found.")
+            return self.not_found("Certificate not found.")
         body = request.data or {}
         try:
             document = _parse_document(body.get("document")) if "document" in body else None
             background = body.get("background")
             if background == "" or background is None:
                 background = request.FILES.get("background")
-            template = services.save_award_template(
+            template = services.save_award_certificate(
                 template,
-                name=body.get("name") if "name" in body else None,
                 document=document,
                 background=background,
             )
@@ -308,8 +300,11 @@ class AwardTemplateDetailsView(RBACView):
         return self.ok(_template_payload(template, request))
 
     def delete(self, request, obj_id: int):
-        template = self._template(obj_id)
+        title = self._title(obj_id)
+        if title is None:
+            return self.not_found("Award title not found.")
+        template = services.get_award_certificate(title)
         if template is None:
-            return self.not_found("Award template not found.")
+            return self.not_found("Certificate not found.")
         template.delete()
         return self.deleted()
