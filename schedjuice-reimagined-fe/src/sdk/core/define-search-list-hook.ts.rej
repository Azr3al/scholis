diff a/schedjuice-reimagined-fe/src/sdk/core/define-search-list-hook.ts b/schedjuice-reimagined-fe/src/sdk/core/define-search-list-hook.ts	(rejected hunks)
@@ -37,10 +37,12 @@ export function useSearchListQuery<T>(
     enabled,
   });
 
+  // React Query v4 has no `isPending`; treat "no completed fetch yet" as loading
+  // so SSR matches the client when fetchStatus is still idle before the first fetch.
   return {
     rows: query.data?.rows ?? [],
     total: query.data?.total ?? 0,
-    isLoading: query.isLoading,
+    isLoading: enabled && !query.isFetched && !query.isError,
     isError: query.isError,
     error: query.error instanceof Error ? query.error : null,
     refetch: () => {
