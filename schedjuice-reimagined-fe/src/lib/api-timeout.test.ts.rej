diff a/schedjuice-reimagined-fe/src/lib/api-timeout.test.ts b/schedjuice-reimagined-fe/src/lib/api-timeout.test.ts	(rejected hunks)
@@ -1,10 +1,22 @@
-import { describe, expect, it } from "vitest";
-
-import { API_CLIENT_TIMEOUT_MS, axiosClient } from "./api";
+import { afterEach, describe, expect, it, vi } from "vitest";
 
 describe("axiosClient timeout", () => {
-  it("fails the request instead of waiting forever when the backend hangs", () => {
+  afterEach(() => {
+    vi.unstubAllEnvs();
+    vi.resetModules();
+  });
+
+  it("uses 10s in production", async () => {
+    vi.stubEnv("NODE_ENV", "production");
+    const { API_CLIENT_TIMEOUT_MS, axiosClient } = await import("./api");
     expect(API_CLIENT_TIMEOUT_MS).toBe(10_000);
-    expect(axiosClient.defaults.timeout).toBe(API_CLIENT_TIMEOUT_MS);
+    expect(axiosClient.defaults.timeout).toBe(10_000);
+  });
+
+  it("uses 30s in development", async () => {
+    vi.stubEnv("NODE_ENV", "development");
+    const { API_CLIENT_TIMEOUT_MS, axiosClient } = await import("./api");
+    expect(API_CLIENT_TIMEOUT_MS).toBe(30_000);
+    expect(axiosClient.defaults.timeout).toBe(30_000);
   });
 });
