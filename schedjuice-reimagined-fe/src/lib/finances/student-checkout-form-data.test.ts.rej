diff a/schedjuice-reimagined-fe/src/lib/finances/student-checkout-form-data.test.ts b/schedjuice-reimagined-fe/src/lib/finances/student-checkout-form-data.test.ts	(rejected hunks)
@@ -43,5 +43,27 @@ describe("buildStudentCheckoutFormData", () => {
       screenshots: [{ file, parsedAmount: "", paymentMethodId: "" }],
     });
     expect(fd.get("screenshot_0_payment_method")).toBeNull();
+    expect(fd.get("payment_method")).toBeNull();
+  });
+
+  it("appends payment_method when preview suggested an id", () => {
+    const single = buildStudentCheckoutFormData({
+      paymentIds: [101],
+      screenshots: [
+        {
+          file,
+          parsedAmount: "50000",
+          paymentMethodId: "7",
+          transactionId: "TXN-1",
+        },
+      ],
+    });
+    expect(single.get("payment_method")).toBe("7");
+
+    const multi = buildStudentCheckoutFormData({
+      paymentIds: [101, 102],
+      screenshots: [{ file, parsedAmount: "100000", paymentMethodId: "7" }],
+    });
+    expect(multi.get("screenshot_0_payment_method")).toBe("7");
   });
 });
