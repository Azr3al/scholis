diff a/schedjuice-reimagined-fe/src/config/course-record-nav.ts b/schedjuice-reimagined-fe/src/config/course-record-nav.ts	(rejected hunks)
@@ -1,4 +1,8 @@
-import { permissionsFor, isStudent } from "@/helpers/authorization";
+import {
+  canAccessCourseAttendance,
+  permissionsFor,
+  isStudent,
+} from "@/helpers/authorization";
 import type { courseType } from "@/types/course";
 import type { organizationType } from "@/types/organization";
 import type { accountType } from "@/types/user";
@@ -64,7 +68,8 @@ export const COURSE_RECORD_NAV_ENTRIES: CourseRecordNavEntry[] = [
     segment: "attendance",
     group: "academic",
     canShow: (user) =>
-      permissionsFor(user).canAny(["attendance.mark", "checkin.view_all"]),
+      canAccessCourseAttendance(user) ||
+      permissionsFor(user).can("checkin.view_all"),
   },
   {
     id: "grading",
