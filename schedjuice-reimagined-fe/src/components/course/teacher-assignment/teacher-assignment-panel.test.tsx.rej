diff a/schedjuice-reimagined-fe/src/components/course/teacher-assignment/teacher-assignment-panel.test.tsx b/schedjuice-reimagined-fe/src/components/course/teacher-assignment/teacher-assignment-panel.test.tsx	(rejected hunks)
@@ -250,7 +250,7 @@ describe("TeacherAssignmentPanel", () => {
     renderPanel();
     await userEvent.click(screen.getByRole("button", { name: /Audrey/ }));
     await userEvent.click(screen.getByRole("button", { name: /^continue$/i }));
-    expect(screen.getByRole("button", { name: /^continue$/i }).disabled).toBe(true);
+    expect(screen.getByRole("button", { name: /^continue$/i })).toBeDisabled();
   });
 
   it("submits weekday-derived sessions without an auto-removal date", async () => {
@@ -258,7 +258,7 @@ describe("TeacherAssignmentPanel", () => {
     await selectTeacherAndRole("Main Teacher");
     await userEvent.click(screen.getByRole("button", { name: /^assign$/i }));
     await waitFor(() => expect(makePostRequest).toHaveBeenCalled());
-    const [url, payload] = makePostRequest.mock.calls[0] as [
+    const [url, payload] = makePostRequest.mock.calls[0] as unknown as [
       string,
       {
         user_id: number;
@@ -284,10 +284,13 @@ describe("TeacherAssignmentPanel", () => {
     await userEvent.click(screen.getByRole("checkbox", { name: /Mon 3/ }));
     await userEvent.click(screen.getByRole("button", { name: /^assign$/i }));
     await waitFor(() => expect(makePostRequest).toHaveBeenCalled());
-    const payload = makePostRequest.mock.calls[0]?.[1] as {
-      assigned_as_role_id: number;
-      substitute_auto_remove_on: string;
-    };
+    const payload = (makePostRequest.mock.calls[0] as unknown as [
+      string,
+      {
+        assigned_as_role_id: number;
+        substitute_auto_remove_on: string;
+      },
+    ])[1];
     expect(payload).toMatchObject({
       assigned_as_role_id: 2,
       substitute_auto_remove_on: "2026-08-03",
@@ -301,16 +304,50 @@ describe("TeacherAssignmentPanel", () => {
     await userEvent.click(screen.getByRole("switch"));
     await userEvent.click(screen.getByRole("button", { name: /^assign$/i }));
     await waitFor(() => expect(makePostRequest).toHaveBeenCalled());
-    const payload = makePostRequest.mock.calls[0]?.[1] as {
-      substitute_auto_remove_on: null;
-    };
+    const payload = (makePostRequest.mock.calls[0] as unknown as [
+      string,
+      {
+        substitute_auto_remove_on: null;
+      },
+    ])[1];
     expect(payload.substitute_auto_remove_on).toBeNull();
   });
 
-  it("blocks assigning with no sessions selected", async () => {
+  it("does not assign when empty-session warning is cancelled", async () => {
     renderPanel();
     await selectTeacherAndRole("Substitute Main Teacher");
-    expect(screen.getByRole("button", { name: /^assign$/i }).disabled).toBe(true);
+    const assign = screen.getByRole("button", { name: /^assign$/i });
+    expect(assign).toBeEnabled();
+    await userEvent.click(assign);
+    expect(
+      screen.getByRole("alertdialog", { name: /assign without sessions/i }),
+    ).toBeTruthy();
+    await userEvent.click(screen.getByRole("button", { name: /^cancel$/i }));
+    expect(makePostRequest).not.toHaveBeenCalled();
+    expect(screen.getByRole("button", { name: /^assign$/i })).toBeEnabled();
+    expect(screen.queryByRole("alertdialog")).toBeNull();
+  });
+
+  it("assigns with no sessions after confirming the warning", async () => {
+    renderPanel();
+    await selectTeacherAndRole("Substitute Main Teacher");
+    await userEvent.click(screen.getByRole("button", { name: /^assign$/i }));
+    await userEvent.click(screen.getByRole("button", { name: /assign anyway/i }));
+    await waitFor(() => expect(makePostRequest).toHaveBeenCalled());
+    const [url, payload] = makePostRequest.mock.calls[0] as unknown as [
+      string,
+      {
+        user_id: number;
+        assigned_as_role_id: number;
+        new_events: { id: number }[];
+      },
+    ];
+    expect(url).toBe("courses/5/assign-events");
+    expect(payload).toMatchObject({
+      user_id: 7,
+      assigned_as_role_id: 2,
+      new_events: [],
+    });
   });
 
   it("assigns a collision-disabled role from the role step without session selection", async () => {
@@ -320,7 +357,7 @@ describe("TeacherAssignmentPanel", () => {
     await userEvent.click(screen.getByRole("radio", { name: "Coordinator" }));
     await userEvent.click(screen.getByRole("button", { name: /^assign$/i }));
     await waitFor(() => expect(makePostRequest).toHaveBeenCalled());
-    const [url, payload] = makePostRequest.mock.calls[0] as [
+    const [url, payload] = makePostRequest.mock.calls[0] as unknown as [
       string,
       {
         user_id: number;
