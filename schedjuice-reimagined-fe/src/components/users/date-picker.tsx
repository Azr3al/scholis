/**
 * Compatibility re-export of the canonical date picker.
 * Prefer importing from `@/components/date/date-picker`.
 * Native browser input is only available via `variant="native"` on that module.
 */
export {
  DatePicker,
  type DatePickerProps,
} from "@/components/date/date-picker";
