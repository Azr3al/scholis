diff a/schedjuice-reimagined-fe/src/components/finances/payment-upload/use-screenshot-part-ocr.ts b/schedjuice-reimagined-fe/src/components/finances/payment-upload/use-screenshot-part-ocr.ts	(rejected hunks)
@@ -32,7 +32,11 @@ export function useScreenshotPartOcr() {
   }, []);
 
   const runOcr = React.useCallback(
-    async (key: string, file: File): Promise<OcrPaymentScreenshotResult | null> => {
+    async (
+      key: string,
+      file: File,
+      options?: { courseId?: string },
+    ): Promise<OcrPaymentScreenshotResult | null> => {
       const requestId = (requestIds.current[key] ?? 0) + 1;
       requestIds.current[key] = requestId;
       setStateByKey((prev) => ({
@@ -41,7 +45,10 @@ export function useScreenshotPartOcr() {
       }));
 
       try {
-        const data = await runPaymentScreenshotOcr({ file });
+        const data = await runPaymentScreenshotOcr({
+          file,
+          courseId: options?.courseId,
+        });
         if (requestIds.current[key] !== requestId) return null;
         setStateByKey((prev) => ({
           ...prev,
