diff a/schedjuice-reimagined-fe/src/components/misc/text-shimmer.tsx b/schedjuice-reimagined-fe/src/components/misc/text-shimmer.tsx	(rejected hunks)
@@ -1,7 +1,6 @@
 "use client";
 
 import { cn } from "@/lib/utils";
-import { motion } from "motion/react";
 import { memo, useMemo } from "react";
 
 export function TextShimmer({
@@ -21,29 +20,17 @@ export function TextShimmer({
   );
 
   return (
-    <motion.span
-      animate={{ backgroundPosition: "0% center" }}
-      className={cn(
-        "relative inline-block bg-[length:250%_100%,auto] bg-clip-text text-transparent",
-        "[--bg:linear-gradient(90deg,#0000_calc(50%-var(--spread)),var(--color-background),#0000_calc(50%+var(--spread)))] [background-repeat:no-repeat,padding-box]",
-        className,
-      )}
-      initial={{ backgroundPosition: "100% center" }}
+    <span
+      className={cn("sj-text-shimmer relative inline-block", className)}
       style={
         {
           "--spread": `${dynamicSpread}px`,
-          backgroundImage:
-            "var(--bg), linear-gradient(var(--color-muted-foreground), var(--color-muted-foreground))",
+          "--shimmer-duration": `${duration}s`,
         } as React.CSSProperties
       }
-      transition={{
-        duration,
-        ease: "linear",
-        repeat: Number.POSITIVE_INFINITY,
-      }}
     >
       {children}
-    </motion.span>
+    </span>
   );
 }
 
