diff a/schedjuice-reimagined-fe/src/components/form/entity-combobox.tsx b/schedjuice-reimagined-fe/src/components/form/entity-combobox.tsx	(rejected hunks)
@@ -16,6 +16,7 @@ import { Field } from "@/components/primitives/field";
 import { Input } from "@/components/primitives/input";
 import { Plus } from "iconoir-react";
 import { parseSchedjuiceApiError } from "@/helpers/schedjuice-api-error";
+import { resolveAllOption } from "./resolve-all-option";
 import { isValidApiEntityIdParam } from "@/helpers/relation-fk";
 import {
   filterToolbarFieldStackClassName,
@@ -138,6 +139,8 @@ type EntityComboboxEntityModeProps = EntityComboboxSharedProps & {
   onSelectedEntityChange?: (entity: any) => void;
   /** Optional first row (e.g. `{ value: "", label: "No category" }`) for nullable FKs. */
   emptyOption?: { value: string; label: string };
+  /** When true, prepends `{ value: "", label: placeholder || "All" }`. A string sets the label. */
+  allOption?: boolean | string;
   /** Button beside the combobox opens a dialog; on success the list refetches and the new id is selected. */
   onCreateNew?: EntityComboboxCreateNewConfig;
   fetchOptions?: never;
@@ -169,6 +172,7 @@ type EntityComboboxFetchModeProps = EntityComboboxSharedProps & {
   customComponent?: never;
   onSelectedEntityChange?: never;
   emptyOption?: never;
+  allOption?: never;
   onCreateNew?: never;
 };
 
@@ -290,6 +294,7 @@ function EntityComboboxEntityMode({
   onSelectedEntityChange,
   containerClassName,
   emptyOption,
+  allOption,
   onCreateNew,
   comboboxPlaceholder,
   comboboxTriggerClassName,
@@ -337,6 +342,13 @@ function EntityComboboxEntityMode({
     },
   });
 
+  const resolvedAllOption = resolveAllOption({
+    emptyOption,
+    allOption,
+    placeholder: comboboxPlaceholder,
+    allowDeselect,
+  });
+
   useEffect(() => {
     if (
       getAllEntities.isSuccess &&
@@ -345,7 +357,7 @@ function EntityComboboxEntityMode({
       !value &&
       renderCount === 0 &&
       canSetDefaultValue &&
-      !emptyOption
+      !resolvedAllOption
     ) {
       const first = getAllEntities.data.data.data[0];
       if (
@@ -382,9 +394,9 @@ function EntityComboboxEntityMode({
         : {}),
     }));
 
-  const options = emptyOption
+  const options = resolvedAllOption
     ? [
-        { value: emptyOption.value, label: emptyOption.label },
+        { value: resolvedAllOption.value, label: resolvedAllOption.label },
         ...(listOptions ?? []),
       ]
     : listOptions;
