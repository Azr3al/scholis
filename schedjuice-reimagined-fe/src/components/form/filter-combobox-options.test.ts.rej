diff a/schedjuice-reimagined-fe/src/components/form/filter-combobox-options.test.ts b/schedjuice-reimagined-fe/src/components/form/filter-combobox-options.test.ts	(rejected hunks)
@@ -24,14 +24,32 @@ describe("filterComboboxOptionsByQuery", () => {
   });
 
   it("matches on label", () => {
-    expect(filterComboboxOptionsByQuery(options, "abel")).toEqual([options[1]]);
-    expect(filterComboboxOptionsByQuery(options, "albina")).toEqual([options[2]]);
+    expect(filterComboboxOptionsByQuery(options, "abel")).toEqual([
+      options[0],
+      options[1],
+    ]);
+    expect(filterComboboxOptionsByQuery(options, "albina")).toEqual([
+      options[0],
+      options[2],
+    ]);
+  });
+
+  it("keeps empty-value rows first when the query matches other items", () => {
+    expect(filterComboboxOptionsByQuery(options, "jul")).toEqual([options[0]]);
+    expect(filterComboboxOptionsByQuery(options, "abel")).toEqual([
+      options[0],
+      options[1],
+    ]);
   });
 
   it("matches on searchText when label would not match", () => {
-    expect(filterComboboxOptionsByQuery(options, "aung@")).toEqual([]);
-    expect(filterComboboxOptionsByQuery(options, "aung aung")).toEqual([options[1]]);
+    expect(filterComboboxOptionsByQuery(options, "aung@")).toEqual([options[0]]);
+    expect(filterComboboxOptionsByQuery(options, "aung aung")).toEqual([
+      options[0],
+      options[1],
+    ]);
     expect(filterComboboxOptionsByQuery(options, "abel@example.com")).toEqual([
+      options[0],
       options[1],
     ]);
   });
