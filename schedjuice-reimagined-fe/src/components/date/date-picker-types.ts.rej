diff a/schedjuice-reimagined-fe/src/components/date/date-picker-types.ts b/schedjuice-reimagined-fe/src/components/date/date-picker-types.ts	(rejected hunks)
@@ -10,6 +10,9 @@ export type SharedDatePickerProps = {
   toDate?: Date | string;
   defaultMonth?: Date | string;
   size?: ControlSize;
+  required?: boolean;
+  /** Defaults to true when `required === false`. Omitted `required` stays uncleared. */
+  clearable?: boolean;
 };
 
 export type PopoverDatePickerProps = SharedDatePickerProps & {
