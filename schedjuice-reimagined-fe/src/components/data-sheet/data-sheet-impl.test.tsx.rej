diff a/schedjuice-reimagined-fe/src/components/data-sheet/data-sheet-impl.test.tsx b/schedjuice-reimagined-fe/src/components/data-sheet/data-sheet-impl.test.tsx	(rejected hunks)
@@ -1,6 +1,11 @@
 import { cleanup, render } from "@testing-library/react";
 import { afterEach, describe, expect, it, vi } from "vitest";
-import { GridCellKind } from "@glideapps/glide-data-grid";
+import {
+  CompactSelection,
+  GridCellKind,
+  type Item,
+  type TextCell,
+} from "@glideapps/glide-data-grid";
 
 const dataEditorSpy = vi.fn();
 
@@ -35,7 +40,7 @@ const baseProps = {
   fieldByColumn: ["email"],
   height: 400,
   menus: { roleLabel: "Test sheet" },
-  getCellContent: () => ({
+  getCellContent: (): TextCell => ({
     kind: GridCellKind.Text,
     data: "",
     displayData: "",
@@ -56,8 +61,11 @@ function lastDataEditorProps() {
   return dataEditorSpy.mock.calls.at(-1)?.[0] as
     | {
         cellActivationBehavior?: string;
-        onCellActivated?: unknown;
-        onCellClicked?: unknown;
+        onCellActivated?: (cell: Item) => void;
+        onCellClicked?: (
+          cell: Item,
+          event: { preventDefault: () => void },
+        ) => void;
       }
     | undefined;
 }
@@ -92,8 +100,8 @@ describe("DataSheet cell activation", () => {
         {...baseProps}
         gridProps={{
           gridSelection: {
-            columns: { length: 0, hasIndex: () => false, hasAll: () => false, toArray: () => [] },
-            rows: { length: 0, hasIndex: () => false, hasAll: () => false, toArray: () => [] },
+            columns: CompactSelection.empty(),
+            rows: CompactSelection.empty(),
           },
           onCellActivated,
         }}
