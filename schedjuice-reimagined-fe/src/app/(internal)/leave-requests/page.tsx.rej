diff a/schedjuice-reimagined-fe/src/app/(internal)/leave-requests/page.tsx b/schedjuice-reimagined-fe/src/app/(internal)/leave-requests/page.tsx	(rejected hunks)
@@ -3,9 +3,11 @@
 import Link from "next/link";
 import { useMemo, useState } from "react";
 
+import { ListRowsSkeleton } from "@/components/loading/structured-skeletons";
 import { PageContainer } from "@/components/layout/page-container";
 import { Button } from "@/components/primitives";
 import { usePageHeader } from "@/components/shell/use-page-header";
+import { formatEnrolledCourses } from "@/lib/leave-requests/format-enrolled-courses";
 import { LeaveRequestStatus } from "@/sdk/_types/leave-requests";
 import { useLeaveRequestsList } from "@/sdk/hooks/leave-requests";
 import { cn } from "@/lib/utils";
@@ -51,6 +53,7 @@ export default function LeaveRequestsPage() {
     status === LeaveRequestStatus.Pending
       ? (list.data?.rows.length ?? 0)
       : undefined;
+  const showListSkeleton = list.isLoading && !list.data;
 
   return (
     <PageContainer width="wide">
@@ -76,10 +79,10 @@ export default function LeaveRequestsPage() {
           ))}
         </div>
 
-        {list.isLoading ? (
-          <p className="text-sm text-muted-foreground" aria-busy="true">
-            Loading leave requests…
-          </p>
+        {showListSkeleton ? (
+          <div aria-busy="true">
+            <ListRowsSkeleton rows={5} />
+          </div>
         ) : null}
 
         {list.isError ? (
@@ -88,7 +91,7 @@ export default function LeaveRequestsPage() {
           </p>
         ) : null}
 
-        {!list.isLoading &&
+        {!showListSkeleton &&
         !list.isError &&
         (list.data?.rows.length ?? 0) === 0 ? (
           <p className="text-sm text-muted-foreground">
@@ -96,34 +99,45 @@ export default function LeaveRequestsPage() {
           </p>
         ) : null}
 
-        <ul className="divide-y divide-border rounded-lg border border-border bg-card">
-          {(list.data?.rows ?? []).map((row) => (
-            <li key={row.id}>
-              <Link
-                href={`/leave-requests/${row.id}`}
-                className={cn(
-                  "flex flex-col gap-1 px-4 py-3 transition-colors",
-                  "hover:bg-muted/40",
-                )}
-              >
-                <div className="flex flex-wrap items-center justify-between gap-2">
-                  <span className="font-medium text-text-primary">
-                    {studentName(row.student)}
-                  </span>
-                  <span className="text-xs uppercase tracking-wide text-muted-foreground">
-                    {row.status}
-                  </span>
-                </div>
-                <span className="text-sm text-text-primary">
-                  {formatDateRange(row.start_date, row.end_date)}
-                </span>
-                <span className="line-clamp-2 text-sm text-muted-foreground">
-                  {row.reason}
-                </span>
-              </Link>
-            </li>
-          ))}
-        </ul>
+        {!showListSkeleton ? (
+          <ul className="divide-y divide-border rounded-lg border border-border bg-card">
+            {(list.data?.rows ?? []).map((row) => {
+              const coursesLabel = formatEnrolledCourses(row.enrolled_courses);
+
+              return (
+                <li key={row.id}>
+                  <Link
+                    href={`/leave-requests/${row.id}`}
+                    className={cn(
+                      "flex flex-col gap-1 px-4 py-3 transition-colors",
+                      "hover:bg-muted/40",
+                    )}
+                  >
+                    <div className="flex flex-wrap items-center justify-between gap-2">
+                      <span className="font-medium text-text-primary">
+                        {studentName(row.student)}
+                      </span>
+                      <span className="text-xs uppercase tracking-wide text-muted-foreground">
+                        {row.status}
+                      </span>
+                    </div>
+                    {coursesLabel ? (
+                      <span className="text-sm text-muted-foreground">
+                        {coursesLabel}
+                      </span>
+                    ) : null}
+                    <span className="text-sm text-text-primary">
+                      {formatDateRange(row.start_date, row.end_date)}
+                    </span>
+                    <span className="line-clamp-2 text-sm text-muted-foreground">
+                      {row.reason}
+                    </span>
+                  </Link>
+                </li>
+              );
+            })}
+          </ul>
+        ) : null}
       </div>
     </PageContainer>
   );
