diff a/schedjuice-reimagined-fe/src/helpers/announcement-center-form-data.ts b/schedjuice-reimagined-fe/src/helpers/announcement-center-form-data.ts	(rejected hunks)
@@ -9,7 +9,7 @@ export type BuildAnnouncementCenterFormDataArgs = {
   dataHtml: string;
   createdById: number | undefined;
   files: File[];
-  courseId?: number;
+  courseIds?: number[];
   sendToMicrosoft?: boolean;
   courseFilters?: AnnouncementCourseFilters;
 };
@@ -20,7 +20,7 @@ export function buildAnnouncementCenterFormData({
   dataHtml,
   createdById,
   files,
-  courseId,
+  courseIds,
   sendToMicrosoft,
   courseFilters,
 }: BuildAnnouncementCenterFormDataArgs): FormData {
@@ -31,8 +31,12 @@ export function buildAnnouncementCenterFormData({
   if (createdById != null) {
     formData.append("created_by", String(createdById));
   }
-  if (scope === AnnouncementCenterScope.PerCourse && courseId != null) {
-    formData.append("course", String(courseId));
+  if (
+    scope === AnnouncementCenterScope.PerCourse &&
+    courseIds != null &&
+    courseIds.length > 0
+  ) {
+    formData.append("course_ids", JSON.stringify(courseIds));
   }
   if (sendToMicrosoft) {
     formData.append("send_to_microsoft", "true");
