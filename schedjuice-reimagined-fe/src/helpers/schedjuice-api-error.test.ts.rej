diff a/schedjuice-reimagined-fe/src/helpers/schedjuice-api-error.test.ts b/schedjuice-reimagined-fe/src/helpers/schedjuice-api-error.test.ts	(rejected hunks)
@@ -1,4 +1,3 @@
-import axios from "axios";
 import { describe, expect, it } from "vitest";
 import { parseSchedjuiceApiError } from "@/helpers/schedjuice-api-error";
 
@@ -20,19 +19,16 @@ describe("parseSchedjuiceApiError payroll_rate_missing", () => {
   });
 
   it("returns payroll_rate_missing code when details are absent", () => {
-    const err = axios.isAxiosError
-      ? ({
-          isAxiosError: true,
-          response: {
-            status: 400,
-            data: {
-              isError: true,
-              message: "payroll_rate_missing",
-            },
-          },
-        } as unknown)
-      : null;
-    Object.defineProperty(err, "isAxiosError", { value: true });
+    const err = {
+      isAxiosError: true,
+      response: {
+        status: 400,
+        data: {
+          isError: true,
+          message: "payroll_rate_missing",
+        },
+      },
+    };
     expect(parseSchedjuiceApiError(err)).toBe("payroll_rate_missing");
   });
 });
