diff a/schedjuice-reimagined-fe/src/helpers/authorization.ts b/schedjuice-reimagined-fe/src/helpers/authorization.ts	(rejected hunks)
@@ -121,6 +121,15 @@ export const canViewPaymentScreenshots = (user: accountType) =>
   (permissionsFor(user).can("payment.view") &&
     permissionsFor(user).can("payment.record"));
 
+export const canMarkAttendance = (user: accountType) =>
+  permissionsFor(user).can("attendance.mark");
+
+export const canViewOwnAttendance = (user: accountType) =>
+  permissionsFor(user).can("attendance.view_own");
+
+export const canAccessCourseAttendance = (user: accountType) =>
+  canMarkAttendance(user) || canViewOwnAttendance(user);
+
 export const canAccessCourseStaffActions = (user: accountType) =>
   permissionsFor(user).canAny([
     "course.update",
@@ -358,6 +367,18 @@ export const canAccessStartingCoursesShortcut = (user: accountType) =>
 export const canAccessUserActivity = (user: accountType) =>
   permissionsFor(user).canAny(["analytics.view", "user.view_all"]);
 
+/** Org settings → Access → single-device mobile login policy. */
+export const canConfigureMobileDevicePolicy = (user: accountType) =>
+  permissionsFor(user).can("mobile_device_policy.configure");
+
+/** User Activity → Mobile devices list and per-user device drill-down. */
+export const canViewMobileDevices = (user: accountType) =>
+  permissionsFor(user).can("mobile_device.view");
+
+/** Sign users out of mobile devices (row actions, bulk revoke). */
+export const canRevokeMobileDevices = (user: accountType) =>
+  permissionsFor(user).can("mobile_device.revoke");
+
 export const isSuperAdmin = (accessor: accountType | undefined | null) => {
   if (!accessor) return false;
   if (hasSuperadminRole(accessor.roles)) return true;
