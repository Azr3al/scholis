{
  "$schema": "https://json.schemastore.org/tsconfig",
  "//": [
    "Path aliases only, for dependency-cruiser's module resolution.",
    "Not used to compile anything.",
    "",
    "Both apps use @/* for their own root. Listing both targets works because",
    "their top-level folders are disjoint — api has server/data/http/lib, web",
    "has app/components. If that ever overlaps, split this into two runs.",
    "",
    "Keep in step with the paths in each app's tsconfig.json."
  ],
  "compilerOptions": {
    "baseUrl": ".",
    "paths": {
      "@/*": ["apps/api/*", "apps/web/*"]
    }
  }
}
